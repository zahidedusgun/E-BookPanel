<!DOCTYPE html>
<html lang="en">
    <%- include(("../partials/head"))  %>
<body>
<%- include("../partials/nav") %>
    <div class="container mt-5">
        <div class="row">
            <div class="col-md-3">
<%-include("../partials/category-menu")  %>
            </div>
        <div class="col-md-9  mt-10">
            <h5><%= title %></h5>
            
            <% books.forEach(books => {%>
                <% if (books.homePage) { %>
                    <%- include("../partials/blog-card",books) %>
                <% } %>
               
            <% }) %>
        <!-- Pagination -->
        <%- include("../partials/pagination") %>
        </div>
    </div>
    fshdfk
</body>
</html>
